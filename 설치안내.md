# 응답 모아 보기 — Cloudflare Workers 설치

Supabase도 되지만 이쪽이 손이 덜 갑니다. 무료 구간이 하루 10만 요청이고,
마스터 페이지까지 워커가 직접 띄워 주니 따로 만들 것이 없습니다.
브라우저만으로 끝나고, 명령줄은 쓰지 않습니다.

## 1. 계정과 워커 만들기

1. dash.cloudflare.com 가입 (무료).
2. 왼쪽 메뉴에서 Workers & Pages → Create → Start with Hello World → Deploy.
3. 이름은 아무거나. 예: `eunho-form`. 배포되면 주소가 나옵니다.
   `https://eunho-form.<계정이름>.workers.dev`

## 2. 코드 넣기

워커 화면에서 Edit code를 눌러 편집기가 열리면, 안에 있던 내용을 전부 지우고
`worker.js` 내용을 붙여 넣습니다. Deploy.

## 3. 저장소(KV) 연결

1. Storage & Databases → KV → Create → 이름 `eunho`.
2. 워커로 돌아가 Settings → Bindings → Add → KV namespace.
   - Variable name: `DB`  (반드시 이 이름)
   - KV namespace: 방금 만든 `eunho`

## 4. 비밀번호 정하기

같은 Settings → Variables and Secrets → Add → Type을 Secret으로.
- 이름: `ADMIN_KEY`
- 값: 아무도 못 맞출 문자열. 예: `eunho-2026-a9f3c1`

Deploy를 한 번 더 누릅니다.

## 5. 설문 페이지에 주소 붙이기

`index.html`을 열어 위쪽 CONFIG에서 이 줄을 찾습니다.

    submit: { url: '', key: '' },

url에 워커 주소 + `/submit`을 넣습니다.

    submit: { url: 'https://eunho-form.<계정이름>.workers.dev/submit', key: '' },

이렇게 바꾸면 페이지가 저절로 달라집니다. 표지와 동의 화면의 문구가
"당신의 휴대전화 안에서만 계산됩니다"에서 "은호에게 전달됩니다"로 바뀌고,
결과 화면의 코드 복사 상자는 사라집니다. **반드시 이 상태로 배포하셔야 합니다.**
전송하면서 전송하지 않는다고 적어 두면 그건 응답자를 속이는 일이고,
성생활·정치·종교는 개인정보 보호법이 말하는 민감정보라 동의 없이 모으면 안 됩니다.

## 6. 응답 보기

    https://eunho-form.<계정이름>.workers.dev/admin?key=eunho-2026-a9f3c1

닉네임, 어디까지 갔는지, 점수, 마지막 시각이 표로 나옵니다.
닉네임을 누르면 그 사람의 답 전체가 보입니다.
중간에 그만둔 사람도 거기까지가 남습니다.

## 7. 한 가지 더

`worker.js` 맨 위 ORIGIN을 배포한 페이지 주소로 좁혀 두면
남이 이 주소로 아무 데이터나 밀어 넣는 것을 조금 막을 수 있습니다.

    const ORIGIN = 'https://leftdrawer.github.io';

## Supabase로 하고 싶다면

1. supabase.com 프로젝트 생성.
2. SQL Editor에서 아래 실행.

```sql
create table responses (
  id text primary key,
  name text,
  at text,
  step int, "of" int,
  done boolean default false,
  total int, grade text, gates jsonb,
  answers jsonb,
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);
alter table responses enable row level security;
create policy "anon upsert" on responses for insert to anon with check (true);
create policy "anon update" on responses for update to anon using (true);
```

3. Settings → API에서 Project URL과 anon key를 받아
   `submit.url`을 `<프로젝트주소>/rest/v1/responses?on_conflict=id`로 두고,
   전송 코드에 `apikey`와 `Prefer: resolution=merge-duplicates` 헤더를 더해야 합니다.
4. 응답은 Supabase 대시보드의 Table Editor에서 봅니다. 마스터 페이지는 따로 만들어야 합니다.

읽기 정책까지 열면 anon key를 가진 누구나 남의 답을 볼 수 있으니, 조회는 대시보드에서만 하십시오.
워커 쪽이 이 문제에서 자유롭습니다.
